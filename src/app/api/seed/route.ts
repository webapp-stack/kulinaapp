import { NextResponse } from 'next/server'
import { db } from '@/lib/db'

// Cukup sisakan kurung siku kosong saja
const sampleProducts = [] 

export async function POST() {
  try {
    // ... sisa kode di bawahnya biarkan saja ...
